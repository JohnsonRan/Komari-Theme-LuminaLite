# Komari-Theme-LuminaLite v1.14.3

## 修复

1. **同一行内的节点卡片高度不再参差不齐**。卡片外层的网格包装被拉到行高，但卡片自身是自适应高度，于是内容多一点的卡片就会比邻居高出一截 —— 绑定了多个 Ping 任务的节点因为多出一行任务标签最明显（约 30px），标签与价格 chip 的差异也能造成几像素的错位。现在卡片会撑满所在行的高度，富余空间落在图表与页脚之间，「在线 / 到期」这条横向扫描带在整行内齐平。大卡片、小卡片、迷你卡片三档均已覆盖。
