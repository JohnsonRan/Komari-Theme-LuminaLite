# Komari-Theme-LuminaLite v1.12.3

## 新增

1. **首页铭牌同时展示主控版本与主题版本**。主控版本（Komari 后端）字号较大，主题版本（LuminaLite）字号较小，hover 可查看完整信息。

## 修复

1. **修复无数据帧的离线节点仍显示在线**。当节点不在 WS data map 中（无新数据上报）但仍在 online 列表时，前端通过 `updated_at` 过期检测主动修正为离线；后端已移出 online 列表的节点也会立即同步状态。
