# Komari-Theme-LuminaLite v2.7.3

## 体验优化

- MiSans 字体在首屏渲染后的空闲阶段尽快加载，移除最长数秒的固定等待
- 修复移动端页面因横滑区域边距与页面 gutter 不一致而产生的横向拖拽
- 保留首页总览、资源指标和地区筛选栏内部的独立横向滚动

## 质量验证

- TypeScript、ESLint、完整 Vitest、生产构建与 `git diff --check` 通过
- 桌面、平板、移动端真实浏览器检查通过
- 移动端视口宽度与文档宽度一致，页面 `scrollX` 保持为 0
