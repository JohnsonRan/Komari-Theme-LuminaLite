## 修复内容

- **GB / TB 量级下磁盘数值不随实时变化更新**：formatBytes 精度档位过低（≥100 无小数、≥10 一位小数、<10 两位小数），磁盘用量的小幅变化（几百 MB / 几 GB）被四舍五入吞掉，图表曲线在动但读数不变。现各档精度提升一位并去除尾随零，如 823 GB → 823.4 GB、45.2 GB → 45.23 GB、1.84 TB → 1.843 TB；整数总量显示不受影响（640 GB 仍为 640 GB）。
