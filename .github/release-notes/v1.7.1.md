# Komari-Theme-LuminaLite v1.7.1

## 更新内容

1. **实时图表悬停暂停**。鼠标移入负载图表区域时，实时数据推送自动冻结，滑动窗口不再逐秒位移——缩放、固定 tooltip 与阅读数值时视图完全稳定；鼠标移出后一次性补回暂停期间的数据点，不丢失任何采样。
2. **修复实时模式缩放/固定随机失效**。此前每秒的数据更新会与滚轮缩放、点击固定产生竞态，导致缩放被重置、固定光标漂离点选位置；悬停暂停后交互期间数据流中断，缩放与固定 100% 生效。
