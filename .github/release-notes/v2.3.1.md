# Komari-Theme-LuminaLite v2.3.1

## 字体一致性修复

- 全局正文、拉丁文字、数字、单位及监控读数统一使用 MiSans Variable。
- 移除 Geist Variable 的字体声明、样式引用与依赖，避免中西文混排及不同组件间出现字重不一致。
- 数字字体变量改为复用全局 MiSans 字体栈，继续保留表格数字特性。
- MiSans 未覆盖的字符仍会回退到系统字体，错误信息与代码内容继续使用系统等宽字体。
